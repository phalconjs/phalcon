const Interface = require('./interface')

class Log {

    /**
     * 
     * @param {*} options 
     */
    constructor(options) {}

    execute() {
        return {
            register: (opts) => {

            }
        }
    }
}