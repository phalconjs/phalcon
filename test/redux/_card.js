const f = [0, 1, 2]
let cmbs = []

for (var index = 0; index < f.length; index++) {
    var element = f[index];
    cmbs.push([element])
    for (var index = 0; index < f..length; index++) {
        var element = array[index];

    }
}